/**
 * Pojedynczy plik do podmiany dekoracyjnego autobusu przewijanego przez całą
 * homepage (`GlobalScrollBus`) — niezależnego od dużego autobusu w hero. Wrzuć
 * plik PNG/SVG pod tą ścieżką w `public/`, a strona zacznie go używać automatycznie
 * (patrz `lib/public-assets.ts`), bez zmian w logice/animacji komponentu.
 */
export const GLOBAL_BUS_ASSET_PATH = "global-bus.png";
